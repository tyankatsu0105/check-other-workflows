name: Action test
on:
  pull_request:
  push:
    branches:
      - main
      - "releases/*"

jobs:
  # build:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v3
  #     - run: |
  #         npm install
  #     - run: |
  #         npm run build
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - run: npm install

      - uses: ./
        with:
          token: ${{ github.token }}
          interval: 10000
